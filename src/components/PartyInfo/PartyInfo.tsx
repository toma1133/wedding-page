import React from 'react'
import styled from 'styled-components'

const PartyInfoContainer = styled.div`
  height: 100vh;
  width: 100vw;
`

const PartyInfo = (): JSX.Element => {
  return <PartyInfoContainer>PartyInfo</PartyInfoContainer>
}

export default PartyInfo
